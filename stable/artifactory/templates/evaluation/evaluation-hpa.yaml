{{- if .Values.evaluation.autoscaling.enabled }}
  {{- if semverCompare ">=v1.23.0-0" .Capabilities.KubeVersion.Version }}
apiVersion: autoscaling/v2
  {{- else }}
apiVersion: autoscaling/v2beta2
  {{- end }}
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: {{ template "artifactory.fullname" . }}
    chart: {{ template "artifactory.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "evaluation.fullname" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "evaluation.fullname" . }}
  minReplicas: {{ .Values.evaluation.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.evaluation.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.evaluation.autoscaling.targetCPUUtilizationPercentage }}
      {{- if .Values.evaluation.autoscaling.metrics }}
{{ tpl (include "evaluation.metrics" .) . | indent 4 }}
      {{- end }}
{{- end }}