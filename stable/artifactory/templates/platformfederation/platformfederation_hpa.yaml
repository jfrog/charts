{{- if and .Values.platformfederation.autoscaling.enabled .Values.platformfederation.enabled }}
  {{- if semverCompare ">=v1.23.0-0" .Capabilities.KubeVersion.Version }}
apiVersion: autoscaling/v2
  {{- else }}
apiVersion: autoscaling/v2beta2
  {{- end }}
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: {{ template "artifactory.fullname" . }}
    chart: {{ template "artifactory.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "platformfederation.fullname" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "platformfederation.fullname" . }}
  minReplicas: {{ .Values.platformfederation.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.platformfederation.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.platformfederation.autoscaling.targetCPUUtilizationPercentage }}
      {{- if .Values.platformfederation.autoscaling.metrics }}
{{ tpl (include "platformfederation.metrics" .) . | indent 4 }}
      {{- end }}
{{- end }}
