{{- if and .Values.jfbus.autoscaling.enabled .Values.jfbus.enabled }}
  {{- if semverCompare ">=v1.23.0-0" .Capabilities.KubeVersion.Version }}
apiVersion: autoscaling/v2
  {{- else }}
apiVersion: autoscaling/v2beta2
  {{- end }}
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: {{ template "artifactory.fullname" . }}
    chart: {{ template "artifactory.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "jfbus.fullname" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "jfbus.fullname" . }}
  minReplicas: {{ .Values.jfbus.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.jfbus.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.jfbus.autoscaling.targetCPUUtilizationPercentage }}
      {{- if .Values.jfbus.autoscaling.metrics }}
{{ tpl (include "jfbus.metrics" .) . | indent 4 }}
      {{- end }}
{{- end }}
