{{- if .Values.apptrust.autoscaling.enabled }}
  {{- if semverCompare ">=v1.23.0-0" .Capabilities.KubeVersion.Version }}
apiVersion: autoscaling/v2
  {{- else }}
apiVersion: autoscaling/v2beta2
  {{- end }}
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: {{ template "artifactory.fullname" . }}
    chart: {{ template "artifactory.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "apptrust.fullname" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "apptrust.fullname" . }}
  minReplicas: {{ .Values.apptrust.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.apptrust.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.apptrust.autoscaling.targetCPUUtilizationPercentage }}
      {{- if .Values.apptrust.autoscaling.metrics }}
{{ tpl (include "apptrust.metrics" .) . | indent 4 }}
      {{- end }}
{{- end }}