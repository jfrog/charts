apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  # Creating required secrets
  - command: ../../../../test/scripts/createEntplusSecret.sh
  - command: kubectl create secret generic rt-secret  --from-file=../unified_secret/artifactory.lic -n ${NAMESPACE}
    ignoreFailure: true
  - command: helm upgrade --install rt jfrog/artifactory-ha --namespace ${NAMESPACE} -f router-sharedenv-values.yaml
timeout: 1400
