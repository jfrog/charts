postgresql:
  persistence:
    enabled: false

mongodb:
  persistence:
    enabled: false
  mongodbRootPassword: password
  mongodbPassword: password

rabbitmq-ha:
  persistentVolume:
    enabled: false

server:
  service:
    type: ClusterIP
