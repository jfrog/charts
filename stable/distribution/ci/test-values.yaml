# CI values for Distribution

# replicaCount: 2
# rbac:
#   create: false
#
# serviceAccount:
#   create: false

postgresql:
  postgresPassword: password
  persistence:
    enabled: false

redis:
  password: "password"
  persistence:
    enabled: false

distributor:
  persistence:
    enabled: false
  loggers:
    - foreman.log

distribution:
  persistence:
    enabled: false
  loggers:
    - access.log
    - request.log
