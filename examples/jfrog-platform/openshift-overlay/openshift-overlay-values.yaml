global:
  jfrogUrl: "https://my-platform.example.com"

# Your own overrides go here — combine with the chart's shipped
# openshift-values.yaml on the command line (that file last), see README.
artifactory:
  artifactory:
    resources:
      requests:
        cpu: "2"
        memory: 4Gi
      limits:
        memory: 8Gi