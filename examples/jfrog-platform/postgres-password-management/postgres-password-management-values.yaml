global:
  jfrogUrl: "https://my-platform.example.com"
  database:
    adminPassword: "<strong-superuser-password>"

postgresql:
  auth:
    postgresPassword: "<strong-superuser-password>"

artifactory:
  database:
    user: artifactory
    password: "<strong-artifactory-password>"

xray:
  enabled: true