nginx:
  https:
    enabled: false

apptrust:
  enabled: true

unifiedpolicy:
  enabled: true

evaluation:
  enabled: true

# Compliance has no dedicated chart value, so it's enabled through the
# merged system.yaml instead.
artifactory:
  extraSystemYaml:
    compliance:
      enabled: true