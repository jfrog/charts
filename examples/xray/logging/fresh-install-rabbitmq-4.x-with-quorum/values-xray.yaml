# An example of fresh install of Xray with RabbitMQ 4.x and Quorum Queues enabled

rabbitmq:
  image:
    tag: 4.1.5-debian-12-r1
  replicaCount: 3
  podManagementPolicy: Parallel

global:
  xray:
    rabbitmq:
      replicaCount: 3
      haQuorum:
        enabled: true