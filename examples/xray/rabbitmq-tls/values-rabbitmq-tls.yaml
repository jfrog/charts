global:
  jfrogUrl: "http://art-artifactory:8082"
  joinKey: EEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEE
  masterKey: FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF
  rabbitmq:
    auth:
      tls:
        enabled: true
        autoGenerated: true

rabbitmq:
  auth:
    # Must differ from the chart's literal default ("password") or the
    # install is refused outright — this is unrelated to TLS itself.
    password: changeme
    tls:
      enabled: true
      autoGenerated: true
